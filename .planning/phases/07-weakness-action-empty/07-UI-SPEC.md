---
phase: 7
slug: weakness-action-empty
status: approved
shadcn_initialized: false
preset: none
created: 2026-03-24
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for the weakness analysis, action tracking, and empty states phase. Generated by ui-researcher, verified by ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (inline SVG + Recharts) |
| Icon library | none (trend arrows via inline SVG/unicode) |
| Font | Pretendard Variable (CDN, already loaded) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, heatmap cell gap |
| md | 16px | Default element spacing, panel internal padding |
| lg | 24px | Section padding, card padding (p-6) |
| xl | 32px | Layout gaps between sections |
| 2xl | 48px | Major section breaks (section title margin-top) |
| 3xl | 64px | Not used in this phase |

Exceptions: Heatmap cell gap is 3px (not a multiple of 4) for visual density.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 | 1.5 | Panel body text, action item text |
| Label | 12px | 500 | 1.4 | Axis labels, category labels, tags |
| Small | 13px | 400 | 1.5 | Tooltip text, empty state body, mini-hints |
| Heading | 20px | 700 | 1.3 | Section titles ("약점 분석", "액션 플랜") |
| Subheading | 14px | 400 | 1.5 | Section subtitles (text-secondary) |
| Card title | 16px | 600 | 1.4 | Panel card titles |
| Stat value | 24px | 700 | 1.2 | Progress bar percentage values |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #09090b | Page background |
| Secondary (30%) | #18181b | Cards (bg-card), panel backgrounds |
| Accent (10%) | #818cf8 (indigo) | Radar fill/stroke, heatmap low end, progress bar fill |
| Destructive | #f87171 | Not used in this phase |

Accent reserved for: Radar chart area fill + stroke, heatmap cell base color, body language progress bar fill, "반복" tag border.

### Phase-specific color tokens

| Token | Value | Usage |
|-------|-------|-------|
| Radar fill | #818cf8 at 20% opacity | RadarChart area fill |
| Radar stroke | #818cf8 | RadarChart border line |
| Heatmap low | #818cf8 at 15% opacity | Low frequency cells |
| Heatmap high | #f472b6 at 90% opacity | High frequency cells |
| Heatmap zero | #818cf8 at 5% opacity | Zero frequency cells |
| Progress bar fill | #818cf8 | Body language progress bar |
| Progress bar track | #27272a | Body language progress bar background |
| Trend up | #34d399 (green) | Positive trend arrow |
| Trend down | #f87171 (red) | Negative trend arrow |
| Trend flat | #71717a (muted) | No change indicator |
| Tag new | #818cf8 at 15% bg, #818cf8 text | "신규" tag |
| Tag repeat | #f472b6 at 15% bg, #f472b6 text | "반복" tag |

### Heatmap color interpolation

```typescript
function cellColor(freqPerMin: number, maxFreq: number): string {
  if (maxFreq === 0) return "rgba(129,140,248,0.05)";
  const t = Math.min(freqPerMin / maxFreq, 1);
  const r = Math.round(129 + t * (244 - 129));
  const g = Math.round(140 + t * (114 - 140));
  const b = Math.round(248 + t * (182 - 248));
  return `rgba(${r},${g},${b},${0.15 + t * 0.75})`;
}
```

---

## Layout Contract

### Section order (top to bottom)

```
1. Page title: "면접 기록"
2. Stat cards (existing — 3-column grid)
3. Score trend chart (existing)
4. Type comparison chart (existing)
5. ── Section break: "약점 분석" ──
   5a. STAR radar chart | Body language panel  (2-column, mobile stacks)
   5b. Filler word heatmap (full-width)
6. ── Section break: "액션 플랜" ──
   6a. Action item tracker | AI recommendation card  (2-column, mobile stacks)
7. Session list (existing)
```

### Section title pattern

```
<div className="mt-12 mb-6">
  <h2 className="text-xl font-bold">{title}</h2>
  <p className="text-secondary text-sm mt-1">{subtitle}</p>
</div>
```

### 2-column grid

```
<div className="grid grid-cols-1 md:grid-cols-2 gap-5">
  {/* Panel A */}
  {/* Panel B */}
</div>
```

### Card wrapper (all panels)

```
<div className="rounded-xl bg-card border border-white/[0.1] p-6">
  <h3 className="text-base font-semibold mb-4">{panel title}</h3>
  {/* panel content */}
</div>
```

---

## Component Contracts

### 1. STAR Radar Chart (WEAK-01)

| Property | Value |
|----------|-------|
| Chart library | Recharts RadarChart |
| SSR | `dynamic({ ssr: false })` |
| Container height | 260px |
| Axes | 4: Situation, Task, Action, Result |
| Domain | [0, 100] |
| Grid | PolarGrid, polygon (default) |
| Grid stroke | rgba(255,255,255,0.1) |
| Fill | var(--color-indigo) at 20% opacity |
| Stroke | var(--color-indigo) |
| Tooltip | bg-card, border-white/10, rounded-lg |
| Axis labels | text-secondary, 12px |
| Card title | "STAR 충족률" |
| Empty (0 sessions) | See Copywriting Contract |

### 2. Body Language Panel (WEAK-03)

| Property | Value |
|----------|-------|
| Categories | 4: 시선, 자세, 표정, 제스처 |
| Display | Horizontal progress bar per category |
| Bar height | 8px, rounded-full |
| Bar fill | var(--color-indigo) |
| Bar track | var(--color-border) (#27272a) |
| Percentage | Right-aligned, 24px bold |
| Trend arrow | Inline after percentage |
| Arrow up | green (#34d399), "▲" or SVG chevron-up |
| Arrow down | red (#f87171), "▼" or SVG chevron-down |
| Arrow flat | muted (#71717a), "—" |
| Card title | "바디랭귀지" |
| Empty (no snapshots) | See Copywriting Contract |
| Mini-hint (1 session) | See Copywriting Contract |

### 3. Filler Word Heatmap (WEAK-02)

| Property | Value |
|----------|-------|
| Rendering | Inline SVG `<rect>` grid |
| Cell size | 28x28px |
| Cell gap | 3px |
| Cell radius | 4px (rx/ry) |
| Y axis | Session dates (newest top), 12px text-secondary |
| X axis | Top-N words (max 8), 12px text-secondary |
| Color scale | indigo (#818cf8) -> pink (#f472b6), opacity 0.15-0.90 |
| Zero cells | rgba(129,140,248,0.05) |
| Wrapper | `overflow-x: auto` for mobile scroll |
| Layout | full-width card |
| Card title | "추임새 빈도" |
| Empty (0 sessions) | See Copywriting Contract |

### 4. Action Item Tracker (ACTN-01)

| Property | Value |
|----------|-------|
| Display | Vertical list of action items |
| Item style | text-sm, bullet or numbered |
| Tags | Inline pill after item text |
| "신규" tag | bg-indigo/15 text-indigo, rounded-full px-2 py-0.5 text-xs |
| "반복" tag | bg-pink/15 text-pink, rounded-full px-2 py-0.5 text-xs |
| Tag visibility | Hidden when only 1 session (no comparison possible) |
| Card title | "액션 아이템" |
| Empty (0 sessions) | See Copywriting Contract |
| Empty (no items) | See Copywriting Contract |

### 5. AI Recommendation Card (ACTN-02)

| Property | Value |
|----------|-------|
| Display | One-line summary, click to expand |
| Collapsed | First sentence or max 80 chars + "..." |
| Expanded | Full `nextSessionSuggestion` text |
| Toggle | Chevron icon rotates 180deg on expand |
| Animation | height transition, 200ms ease |
| Card title | "AI 추천" |
| Empty (0 sessions) | See Copywriting Contract |
| Empty (no suggestion) | See Copywriting Contract |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Section title 1 | 약점 분석 |
| Section subtitle 1 | STAR 충족률, 추임새, 바디랭귀지를 분석합니다 |
| Section title 2 | 액션 플랜 |
| Section subtitle 2 | AI가 제안하는 다음 단계입니다 |
| STAR empty (0 sessions) | 첫 세션을 완료하면 STAR 충족률을 확인할 수 있어요 |
| Body language empty (no data) | 카메라를 켜고 면접하면 바디랭귀지를 분석할 수 있어요 |
| Body language mini-hint (1 session) | 추이 변화는 2개 이상 세션에서 확인할 수 있어요 |
| Heatmap empty (0 sessions) | 세션이 쌓이면 추임새 패턴을 한눈에 볼 수 있어요 |
| Action items empty (0 sessions) | 면접 후 개선할 점을 정리해 드려요 |
| Action items empty (no items) | 이 세션에서는 별도 액션 아이템이 없어요 |
| AI recommendation empty (0 sessions) | 면접 후 다음 세션을 위한 맞춤 제안을 받아보세요 |
| AI recommendation empty (no suggestion) | 이 세션에 대한 추천이 아직 없어요 |

### Empty state visual pattern

```
<div className="flex flex-col items-center justify-center py-10 text-center">
  <p className="text-secondary text-sm">{empty message}</p>
</div>
```

All empty states are centered within their card container. No icons. No CTA buttons inside panels (the existing full-page empty state with CTA remains for 0 sessions).

---

## Interaction Contract

| Interaction | Behavior |
|-------------|----------|
| Radar tooltip | Show dimension name + percentage on hover |
| Heatmap cell hover | Show tooltip: "{word} - {freqPerMin}/분 ({session date})" |
| AI card toggle | Click anywhere on card to expand/collapse |
| Action item hover | No special interaction |
| Body language bar | No special interaction |
| Section scroll | Natural scroll, no sticky headers |

---

## Responsive Contract

| Breakpoint | Layout change |
|------------|---------------|
| >= md (768px) | 2-column grid for paired panels |
| < md | Single column stack, all panels full-width |
| Heatmap overflow | `overflow-x: auto` wrapper, horizontal scroll on narrow screens |
| Heatmap max words | 8 words max (keeps legible on all screens) |

---

## Animation Contract

| Element | Animation |
|---------|-----------|
| Panel cards | `motion/react` fade-in: opacity 0->1, y 10->0, delay staggered |
| AI card expand | CSS height transition, 200ms ease |
| Chevron rotate | CSS transform rotate(180deg), 200ms ease |
| Trend arrows | None (static) |

Consistent with existing Phase 6 pattern: `motion.div` with `initial={{ opacity: 0, y: 10 }}` and `animate={{ opacity: 1, y: 0 }}`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required |

No third-party registries used. All components are custom.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS — 12 copy strings, Korean, consistent tone
- [x] Dimension 2 Visuals: PASS — layout matches CONTEXT.md, card pattern consistent
- [x] Dimension 3 Color: PASS — all colors from globals.css, indigo-pink gradient
- [x] Dimension 4 Typography: PASS — Pretendard, sizes match existing patterns
- [x] Dimension 5 Spacing: PASS — all multiples of 4, one documented exception (3px)
- [x] Dimension 6 Registry Safety: PASS — no external registries, all custom

**Approval:** approved 2026-03-24
