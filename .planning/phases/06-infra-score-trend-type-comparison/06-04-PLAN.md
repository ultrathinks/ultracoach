---
phase: 6
plan: "04"
title: "Type Comparison Chart Widget"
wave: 2
depends_on: ["06-01"]
requirements_addressed: [COMP-01, INFR-03]
files_modified:
  - src/widgets/history/type-comparison-chart.tsx
autonomous: true
---

## Objective

Build the Recharts grouped `BarChart` component that compares average delivery and content scores across interview types (인성/기술/컬처핏). Each type group shows two bars side by side (indigo for delivery, pink for content). Wrapped in `dynamic({ ssr: false })` per INFR-03.

## Tasks

<task id="4.1">
<objective>Create the type comparison bar chart widget</objective>
<read_first>
- src/entities/analytics/types.ts (TypeComparisonGroup interface -- type, typeLabel, avgDelivery, avgContent, count)
- src/widgets/history/score-trend-chart.tsx (sibling chart pattern: "use client", CSS variables, card wrapper, Tooltip style)
- src/app/globals.css (CSS variables: --color-indigo, --color-pink, --color-card, --color-muted, --color-foreground)
- .planning/phases/06-infra-score-trend-type-comparison/06-CONTEXT.md (decisions: grouped bar chart, indigo+pink, only render types that have data)
</read_first>
<action>
Create `src/widgets/history/type-comparison-chart.tsx`:

```typescript
"use client";

import type { TypeComparisonGroup } from "@/entities/analytics";
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  Tooltip,
  ResponsiveContainer,
} from "recharts";

interface TypeComparisonChartProps {
  data: TypeComparisonGroup[];
}

function TypeComparisonChartInner({ data }: TypeComparisonChartProps) {
  if (data.length === 0) return null;

  const chartData = data.map((d) => ({
    name: d.typeLabel,
    전달력: d.avgDelivery,
    답변력: d.avgContent,
    count: d.count,
  }));

  return (
    <div className="rounded-xl bg-card border border-white/[0.1] p-6">
      <h3 className="font-semibold mb-6">유형별 비교</h3>
      <ResponsiveContainer width="100%" height={240}>
        <BarChart data={chartData} barGap={4} barCategoryGap="30%">
          <XAxis
            dataKey="name"
            stroke="var(--color-muted)"
            fontSize={13}
            tickLine={false}
            axisLine={false}
          />
          <YAxis
            domain={[0, 100]}
            stroke="var(--color-muted)"
            fontSize={12}
            tickLine={false}
            axisLine={false}
            width={30}
          />
          <Tooltip
            contentStyle={{
              backgroundColor: "var(--color-card)",
              border: "1px solid rgba(255,255,255,0.1)",
              borderRadius: "8px",
              color: "var(--color-foreground)",
              fontSize: "13px",
            }}
          />
          <Bar
            dataKey="전달력"
            fill="var(--color-indigo)"
            radius={[4, 4, 0, 0]}
            maxBarSize={40}
          />
          <Bar
            dataKey="답변력"
            fill="var(--color-pink)"
            radius={[4, 4, 0, 0]}
            maxBarSize={40}
          />
        </BarChart>
      </ResponsiveContainer>
      <div className="flex gap-6 mt-5 text-sm text-secondary">
        <span className="flex items-center gap-2">
          <span className="w-2.5 h-2.5 rounded-full bg-indigo" />
          전달력
        </span>
        <span className="flex items-center gap-2">
          <span className="w-2.5 h-2.5 rounded-full bg-pink" />
          답변력
        </span>
      </div>
    </div>
  );
}

export { TypeComparisonChartInner };
```

Dynamic import pattern (used in consuming widget, Plan 06-05):

```typescript
const TypeComparisonChart = dynamic(
  () => import("@/widgets/history/type-comparison-chart").then((m) => ({ default: m.TypeComparisonChartInner })),
  { ssr: false },
);
```

Key implementation details:
- `barGap={4}` -- small gap between paired bars within a group
- `barCategoryGap="30%"` -- space between type groups
- `radius={[4, 4, 0, 0]}` -- rounded top corners matching the card border-radius aesthetic
- `maxBarSize={40}` -- prevents bars from stretching too wide when only 1 type exists
- Only types with data are rendered (empty groups are not in the data array per compute layer)
- Legend is manual HTML matching score-trend-chart legend pattern
- Korean data keys (`전달력`, `답변력`) for automatic Tooltip labeling
</action>
<acceptance_criteria>
- grep "use client" src/widgets/history/type-comparison-chart.tsx returns match at line 1
- grep "TypeComparisonChartInner" src/widgets/history/type-comparison-chart.tsx returns at least 2 matches
- grep "BarChart" src/widgets/history/type-comparison-chart.tsx returns at least 1 match
- grep "var(--color-indigo)" src/widgets/history/type-comparison-chart.tsx returns at least 1 match
- grep "var(--color-pink)" src/widgets/history/type-comparison-chart.tsx returns at least 1 match
- grep "유형별 비교" src/widgets/history/type-comparison-chart.tsx returns 1 match
- grep -c " as " src/widgets/history/type-comparison-chart.tsx returns 0
</acceptance_criteria>
</task>

## Verification

```bash
# Component exists
grep "export.*TypeComparisonChartInner" src/widgets/history/type-comparison-chart.tsx

# Uses recharts BarChart
grep "from \"recharts\"" src/widgets/history/type-comparison-chart.tsx

# No hardcoded hex colors
grep -c "#[0-9a-fA-F]" src/widgets/history/type-comparison-chart.tsx
# Expected: 0

# Build succeeds
pnpm build 2>&1 | tail -5
```

## must_haves

- Grouped bar chart: two bars (전달력/답변력) per interview type
- Uses `var(--color-indigo)` and `var(--color-pink)` -- no hardcoded hex
- `"use client"` directive present
- Named export for dynamic import compatibility
- Returns null when data is empty (no error, no broken chart)
